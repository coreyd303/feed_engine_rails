<div class="row">
  <div class="col-md-9" id="trip">
    <div class="panel" id="trip">
      <div class="panel-heading">
        <h1><%= @trip.name %></h1>
        <%= @trip.description %>
        <%= @trip.date.strftime('%A, %b %d') %>
        <%= @trip.trip_location %>
      </div>
    </div>
    <div class="col-md-5">
      <div class="thumbnail" id="feed">
        <h1>Twitter Feed</h1>
      </div>
    </div>
      <div class="col-md-7">
    <div class="thumbnail" id="feed">
      <h1>Some Instas</h1>
    </div>
    </div>

  </div>
  <div class="col-md-3">
    <div class="sidebar-offcanvas">
      <div class="list-group">
      <% if current_user %>
        <li class="list-group-item" id="trip"><h2>Invite friends</h2></li>
        <%= render partial: 'email_form' %>
      <% end %>
        <li class="list-group-item" id="trip"><h2>Who's going</h2></li>
        <% @trip.users.each do |user| %>
          <li class="list-group-item" id="trip"><h4><%= link_to user.name, user_path(user) %></h4></li>
        <% end %>
    </div>
  </div>
</div>
  
